from collections import deque

graph = {
    "A" : ["B", "C"],
    "B" : ["D", "E"],
    "C" : ["F"],
    "D" : [],
    "E" : ["F"],
    "F" : []
}

def bfs (start):
    visited = set()
    queue = deque([start])

    while queue:
        vertex_kdm = queue.popleft()
        if vertex_kdm not in visited:
            print(vertex_kdm)
            visited.add(vertex_kdm)
            queue.extend(graph[vertex_kdm])

bfs("A")